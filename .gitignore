# ============ 发布面 / 本地工程面 分离 ============
# 仓库根目录只放发布到 GitHub / npm 的文件：
#   index.mjs / convert.mjs / cordis.patch.yml / package.json / README.md / LICENSE / test/
# 本地工程文件一律收进 dev/（交接文档、增长策略、本地脚本/夹具），不入库、不进 npm 包。

# 本地工程面 —— 整个目录不入库
dev/

# 本地测试依赖（node_modules junction 到 DSH profile）
node_modules/

# dsh-file-claim 插件运行时目录（多会话并发协调，本地状态不入库）
.dsh-file-claim/

# 兜底：根目录若再出现本地会话快照，同样不入库
.prev-session*.jsonl

# 编辑器 / 系统噪音
.DS_Store
*.log

# npm pack 发布产物（上传到 GitHub Release，不入库）
*.tgz
